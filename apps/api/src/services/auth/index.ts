// Auth library (Better Auth instance)
export { auth } from './auth.lib';

// Auth middleware
export { jwtAuth, requireAdmin } from './auth.middleware';

// Auth routes (will be created in future - currently inline in routes/auth.routes.ts)
